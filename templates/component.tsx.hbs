import React from 'react';
import './styles/{{dashCase name}}-styles.scss';

export const {{pascalCase name}}: React.FC = () => {
return (
<div className="{{dashCase name}}">{{pascalCase name}}</div>
);
};
